<h4>Featured on:</h4>
<% unless @networks.blank? %>
  <table class="table">
    <tr>
      <th>Network</th>
      <th>Impressions</th>
      <th>Views</th>
      <th>Royalties</th>
    </tr>

    <% @networks.each do |n| %>
      <tr>
        <td><%= link_to_network n %></td>
        <td><%= n.count_impressions(@video) %></td>
        <td></td>
        <td></td>
      </tr>
    <% end %>
  </table>
<% else %>
  <p>This video has no network impressions</p>
<% end %>